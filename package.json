{
  "name": "to-es6-template-literal",
  "displayName": "to-es6-template-literal",
  "description": "",
  "version": "0.0.1",
  "publisher": "jansav",
  "engines": {
    "vscode": "^1.27.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:to-es6-template-literal.format"
  ],
  "main": "src/index",
  "contributes": {
    "commands": [
      {
        "command": "to-es6-template-literal.format",
        "title": "Convert text to ES6 template literal"
      }
    ],
    "keybindings": [
      {
        "command": "to-es6-template-literal.format",
        "key": "ctrl+k",
        "mac": "cmd+k",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "jest"
  },
  "dependencies": {
    "jest": "^23.6.0",
    "templatize-string": "^1.0.3"
  },
  "devDependencies": {
    "eslint": "^4.11.0",
    "eslint-plugin-jest": "^21.22.1",
    "vscode": "^1.1.21"
  }
}
