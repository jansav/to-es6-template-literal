{
  "name": "templatize-string",
  "displayName": "templatize-string",
  "description": "",
  "version": "0.0.1",
  "publisher": "jansav",
  "engines": {
    "vscode": "^1.27.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:templatize-string.fetch"
  ],
  "main": "./main",
  "contributes": {
    "commands": [
      {
        "command": "templatize-string.fetch",
        "title": "Turn concatenated string to ES6 string template"
      }
    ]
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "jest"
  },
  "dependencies": {
    "jest": "^23.6.0",
    "templatize-string": "^1.0.3"
  },
  "devDependencies": {
    "eslint": "^4.11.0",
    "eslint-plugin-jest": "^21.22.1",
    "vscode": "^1.1.21"
  }
}
